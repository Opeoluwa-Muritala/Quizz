services:
  quiz:
    image: nginx:1.27-alpine
    container_name: corporate-quiz
    ports:
      - "80:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
